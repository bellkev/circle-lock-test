dependencies:
  override:
    - curl -L -o ~/bin/jq https://github.com/stedolan/jq/releases/download/jq-1.5rc1/jq-linux-x86_64-static
test:
  override:
    - bin/do-exclusively-on-branch all-bash sleep 120
